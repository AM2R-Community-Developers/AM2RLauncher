In here, you will find an appdata, .desktop file and an icon if you wish to redistribute the AM2RLauncher to somewhere else.
It should also be noted, that you can use the `NoAutoUpdate` configuration, in order to build the AM2RLauncher with the auto-updating features disabled.
